for installation:

curl -fsSL "https://raw.githubusercontent.com/murrou-cell/hyprarch/main/install.sh?$(date +%s)" \
  -o /tmp/hyprarch-install.sh \
  && chmod +x /tmp/hyprarch-install.sh \
  && /tmp/hyprarch-install.sh
